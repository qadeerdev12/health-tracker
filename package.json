{
  "name": "health-tracker",
  "version": "0.1.0",
  "private": true,
  "description": "Health tracking app. Express server holding the WHOOP integration; the app lives in mobile/.",
  "type": "module",
  "main": "server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.115.0",
    "dotenv": "16.4.7",
    "express": "5.2.1"
  }
}
